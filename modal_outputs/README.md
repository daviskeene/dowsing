# Modal outputs

Local downloads from Modal runs are written here.

Example:

```bash
modal run modal_run.py --mode full --run-name full-l4-3000-robustness
```

This creates:

```text
modal_outputs/{run_name}-dowsing-results.tar.gz
modal_outputs/{run_name}/
```

These generated downloads are ignored by git.

