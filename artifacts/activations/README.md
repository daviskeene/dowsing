# Activation artifacts

Generated by:

```bash
make collect
```

Expected shape:

```text
artifacts/activations/{set_name}/metrics.csv
artifacts/activations/{set_name}/layer_00.npy
...
artifacts/activations/{set_name}/layer_05.npy
```

These files are ignored by git because they are large intermediate artifacts.

